%h3 My Reservations
- if @member.reservations.any?
  - @member.reservations.each do |r|
    = r.schedule.activity.title
    = r.schedule.date
    = r.schedule.time_range
    = "You booked for #{r.participant_count} person(s)."
    %hr
- else
  You need to start your reservation!
